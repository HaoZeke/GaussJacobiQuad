# DEPRECATED — changelogs are generated by cocogitto (`cog changelog` / `cog bump`).
# See RELEASE.md and cog.toml. News fragments are no longer collected here;
# write conventional commits instead and let `cog` render NEWS.md.
#
# Historical content lives in NEWS.md. Pending fragments from newsfragments/
# were migrated into the "Unreleased" section of NEWS.md.
