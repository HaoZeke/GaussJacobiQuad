# Packaging is driven from the repository root (../pyproject.toml + ../meson.build
# with -Dpython_package=true) so the sdist includes Fortran sources and git metadata.
# Do not configure this directory as a standalone meson-python project.
error('Build the Python package from the repository root with meson-python, not python/')
